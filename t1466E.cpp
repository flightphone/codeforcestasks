#include <stdio.h>
#include <vector>
using namespace std;

const int mod = 1000000007;

int main()
{
    /*
    freopen("input.txt", "rt", stdin);
    freopen("output.txt", "wt", stdout);
    int T;
    scanf("%d", &T);
    while (T--)
    {
        long n;
        scanf("%ld", &n);
        vector<unsigned long long> x(n);
        for (long i = 0; i < n; i++)
        {
            
            scanf("%ulld", &x[i]);
            
        }
    */    
        unsigned long long r = 105;
        unsigned long long a = 35;
        unsigned long long c = a & r;
        printf("%u\n", c);
    //}
}